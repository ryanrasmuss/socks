CC = gcc
AR = ar
ARFLAGS = rcs
CFLAGS = -g -Werror -Wall

SRCS = socks.c
OBJS = socks.o

LIBR = socks.a

all: $(LIBR)

$(LIBR): $(OBJS)
	$(AR) $(ARFLAGS) $@ $(OBJS)

$(OBJS): %.o : %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -v $(OBJS)
	rm -v $(LIBR)
